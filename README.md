# C Test Practice

## Generate Tests

testgen.py takes .txt files from input and generates .json in output. 
Move files to /public/tests/

## Serve public directory with python webserver

```bash
cd public
python3 -m http.server 8000
```

Access at http://localhost:8000